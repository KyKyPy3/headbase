<div class="center">
	<div style="float:left;width:750px;margin-bottom:1em;margin-top:2em;">
		<div id="form-container">
			<h1>Авторизация</h1>
			<h2> Заполните пожалуйста форму регистрации</h2>
			
			<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
				<p><%= f.label :email %>
				<%= f.email_field :email %></p>
				
				<p><%= f.label :password %>
				<%= f.password_field :password %></p>
				
				<% if devise_mapping.rememberable? -%>
					<p><%= f.label :remember_me %><%= f.check_box :remember_me, :style => "margin-top:.8em;" %></p>
				<% end -%>
				
				<p class="submit"><%= f.submit "Sign in", :class => "signin_submit" %></p>
			<% end %>
			
			<%= render :partial => "devise/shared/links" %>
		</div>
	</div>
</div>
<div class="clear"></div>